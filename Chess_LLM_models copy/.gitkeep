Drop trained .pth chess checkpoints in this folder.
They are gitignored (see ../.gitignore) so they won't be committed.
After running ../convert_pth_to_onnx.py, the resulting .onnx files land here too.
